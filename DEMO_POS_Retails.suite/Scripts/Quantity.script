Put everyimagelocation("itemDetail") into addeditems
Put the number of items in addeditems into lineitems

If lineitems > 0
	Click item random(lineitems) of addeditems
	Click "QuantityBtn"
	
	TypeText random(5), returnKey
End If

